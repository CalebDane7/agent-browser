---
name: agent-browser
description: "Operate the user's real authenticated Windows Stable Chrome for websites, rendered UI, screenshots, forms, uploads and existing-tab help. Use when saved logins, profile identity or visible browser state affect the result. Uses the runtime-configured default account unless another enrolled account is requested. Not for information-only public web research or pure API/source checks."
---

# Agent Browser

Before running any browser command, read the canonical
[Agent Browser operator skill](skills/agent-browser/SKILL.md) and follow its
account, session, ownership, cleanup, and supported-command instructions.

This repository entrypoint routes to that maintained payload; it does not
supply a separate set of browser instructions.
